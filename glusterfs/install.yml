- hosts: gluster 
  tasks:
    - name : 'YUM updating packages'
      become: true
      yum: name='*' state=latest
    - name: 'YUM install base software'
      become: true
      yum:
        name: "{{ item }}" 
        state: latest
      with_items:
        - yum-utils
        - net-tools
        - wget  
        - vim  
        - telnet  
        - epel-release  
        - glusterfs  
